name: Docker Image CI

on:
  push:
    tags: [ "v*" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: ./biuld.sh
    - name: Push
      run: docker images
